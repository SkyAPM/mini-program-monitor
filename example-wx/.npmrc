# WeChat's "Build npm" does not follow symlinks, so the default behavior
# of `file:` dependencies (symlink) breaks the example. Force npm to copy
# the linked package into node_modules as a real directory.
install-links=true
